// // src/services/incidenciaService.js
// import { http } from "./httpClient";

// const BASE_PATH = "/incidencias";

// export const obtenerIncidenciasAdmin = (params = {}) =>
//     http.get(BASE_PATH, params);

// export const obtenerMisIncidencias = (params = {}) =>
//     http.get(`${BASE_PATH}/mias`, params);

// export const obtenerIncidencia = (id) =>
//     http.get(`${BASE_PATH}/${id}`);

// export const crearIncidencia = (payload) =>
//     http.post(BASE_PATH, payload);

// export const actualizarIncidencia = (id, payload) =>
//     http.put(`${BASE_PATH}/${id}`, payload);

// export const eliminarIncidencia = (id) =>
//     http.del(`${BASE_PATH}/${id}`);
